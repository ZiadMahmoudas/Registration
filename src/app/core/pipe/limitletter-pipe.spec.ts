import { LimitletterPipe } from './limitletter-pipe';

describe('LimitletterPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitletterPipe();
    expect(pipe).toBeTruthy();
  });
});
